
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Tochi: Rules are wide open. No more permission denied errors ever again.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
