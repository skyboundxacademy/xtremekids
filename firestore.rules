
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Simple English: If you're logged in, you can learn, talk, and build!
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
