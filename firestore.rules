
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // Simplest English: Allow everyone to read and write everything.
      // No barriers for Tochi's dream! Whether verified or not.
      allow read, write: if true;
    }
  }
}
