
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Tochi: Simplified English - Rules are wide open so your app never blocks anyone again.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
