
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // Wide open rules as requested by Tochi.
      // Allows all students and admins to build the dream without barriers.
      // No more "Missing or insufficient permissions" errors.
      allow read, write: if true;
    }
  }
}
