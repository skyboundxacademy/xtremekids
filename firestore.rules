
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // Wide open rules for Tochi's dream. 
      // No more "Permission Denied" errors for any user.
      allow read, write: if true;
    }
  }
}
