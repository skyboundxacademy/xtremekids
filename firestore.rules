
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // Indestructible Dream Rules: Wide open for Tochi's pioneers.
      // No more "Permission Denied" errors for any user, ever.
      allow read, write: if true;
    }
  }
}
